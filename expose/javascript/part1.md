1. values added: 20
2. final result: 20
3. We should not use var because var ignores code blocks and are function scoped. This means in the while in the same function, that varaiable can be accessed at any time, regardless of any code blocks like if/else or for statements.
4. values added: 20
5. This returns an error. The reason is beacuse let is block scoped, meaning it cannot be accessed outside of any code block. This includes any if/else statements. 
6. This returns an error because const variables cannot be reassigned. This does on line 7
7. This returns an error because const variables cannot be reassigned. This does on line 7